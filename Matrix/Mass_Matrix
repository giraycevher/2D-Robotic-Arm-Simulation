function [M_matris]=Mass_matris(tetha2,L1,L2,m1,m2)
l1=L1/2; 
l2=L2/2; 

c2=cos(tetha2);
%radian
M11 = m1*l1^2 + m2*(L1^2 + l2^2 + 2*L1*l2*c2);
M12 = m2*(l2^2 + L1*l2*c2);
M21 = M12;
M22 = m2*l2^2; 
M_matris=[M11 M12;M21 M22];
end
